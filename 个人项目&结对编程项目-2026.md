# 宿舍账单分摊管家（Web 应用版）项目任务书

> 课程名称：《软件工程导论》
>
> 开发形式：个人项目（第 1 周）+ 结对项目（第 2 周）
>
> 项目类型：Web 应用

## 一、项目目标

开发一个宿舍公共账单管理 Web 应用，实现账本、成员、账单、分摊和结算管理。可使用AI编程实现，但是必须读懂代码。个人项目和结对编程项目需到指定助教处当场演示，并回答助教指定的至少4处代码的逻辑和用途。

项目分为两个阶段：

| 阶段   | 形式     | 主要任务                              |
| ---- | ------ | --------------------------------- |
| 个人项目 | 一人独立完成 | 完成后端业务逻辑、文件存储、RESTful API、均摊算法和测试 |
| 结对项目 | 两人结对完成 | 在个人项目后端基础上，选择其中一人的实现，完成前端页面和交互    |

结对项目原则上不新增或重新实现后端功能，只允许修复接口缺陷和联调问题，个人项目、结对项目都需要使用头歌平台对项目进行版本控制。

## 二、核心业务规则

### 2.1 账单模型

一笔账单包括：

- 所属账本；
- 名称；
- 总金额；
- 记账日期；
- 分摊方式；
- 参与人列表；
- 垫付人；
- 按天分摊周期或指定权重。

金额统一使用整数“分”存储，金额字段使用 `Cents` 后缀。

### 2.2 分摊方式

| 方式   | 代码          | 规则              |
| ---- | ----------- | --------------- |
| 均摊   | `EVEN`      | 所有参与人平均分摊       |
| 按天摊  | `BY_DAYS`   | 按账单周期内的有效在住天数分摊 |
| 指定份额 | `BY_WEIGHT` | 按参与人的正整数权重分摊    |

### 2.3 有效在住天数

仅用于 `BY_DAYS`。

有效天数为账单周期与成员在住区间的交集天数，入住日和退宿日均计入。`leaveDate` 为空表示仍在住。

有效天数为 0 的参与人分摊 0 分。

### 2.4 误差守恒

使用最大余数法处理不能整除的金额：

1. 计算每人的精确份额；
2. 每人先取向下取整值；
3. 计算剩余金额；
4. 按小数部分从大到小依次补 1 分；
5. 小数部分相同时，按 `participants` 中的顺序优先。

必须满足：

```
所有参与人的分摊金额之和 = 账单总金额
```

### 2.5 净余额与结算

```
净余额 = 累计垫付金额 - 累计分摊金额
```

- 净余额大于 0：应收；
- 净余额小于 0：应付；
- 所有成员净余额之和必须为 0；
- 结算方案必须使所有成员结算后的余额归零；
- 最少转账方案必须保证转账笔数最少；
- `payerId` 必须属于 `participants`。

## 三、项目安排与技术约束

### 3.1 两阶段安排

| 项目   | 个人项目                 | 结对项目             |
| ---- | -------------------- | ---------------- |
| 开发内容 | 后端功能                 | 前端页面与交互          |
| 数据存储 | JSON、JSONL 等文本文件     | 调用个人项目提供的 API    |
| 核心要求 | RESTful API、算法、存储、测试 | 页面完整、交互清晰、接口联调正确 |
| 验收方式 | 自动化测试和接口测试           | 页面操作、接口联调和回归测试   |

### 3.2 后端技术约束

1. 后端提供给前端调用的接口必须采用 RESTful 风格，编程语言不限。
2. 可使用 Flask、FastAPI、Express、Koa 等轻量 Web 框架。
3. 禁止使用数据库，包括 SQLite、MySQL、PostgreSQL、MongoDB、Redis 等。
4. 数据必须保存在 JSON、JSONL、CSV 等文本文件中。
5. 算法层、存储层和 其它接口层必须分离。
6. 算法函数不得直接读写文件或依赖 HTTP 请求对象。
7. 金额统一使用整数“分”，不得使用浮点数直接保存金额。
8. 必须提供一条命令运行全部测试。
9. 多账本及账本下的成员、账单数据必须相互隔离。

### 3.3 前端技术约束

1. 可使用原生 HTML、CSS、JavaScript，也可使用 Vue、React 等前端框架。
2. 前端必须通过 HTTP API 获取和修改数据，不得直接读取数据文件。
3. 页面必须包含加载中、无数据、操作成功和操作失败等状态。
4. 表单必须进行必要的前端校验，但以后端校验结果为最终依据。
5. 页面设计必须适配主流手机竖屏的屏幕比例和大小。
6. 前端技术选择和启动方式应在 README 中说明。

## 四、个人项目：后端功能

### 4.1 功能范围

- **必做：F1、F2、F3、F4、F5、F6、F7、F8、F9**
- **选做：F10、F11、F12**

#### F1 成员管理（必做）

- 新增成员：姓名必填且不超过 10 个字符；
- 入住日期必填；
- 退宿日期可空，但不得早于入住日期；
- 查询指定账本的成员列表；
- 修改成员信息；
- 成员参加过账单时不得直接删除，应返回明确错误；
- 编辑入住或退宿日期后，相关按天账单应按最新信息重新计算。

#### F2 账单录入（必做）

- 名称必填且不超过 20 个字符；
- 金额必须为正数且最多两位小数(保存到文本文件时使用单位分)；
- 记账日期必填；
- 参与人至少 1 人；
- 垫付人必须属于参与人；
- `BY_DAYS` 必须提供合法周期；
- `BY_WEIGHT` 必须为每位参与人提供正整数权重；
- 提供保存前的分摊预览接口；
- 非法请求返回合适的 HTTP 状态码和错误信息。

#### F3 分摊计算（必做）

- 正确实现 `EVEN`、`BY_DAYS`、`BY_WEIGHT`；
- 按最大余数法处理金额误差；
- 满足金额守恒；
- 通过官方测试 T01–T10。

#### F4 账单查询与维护（必做）

- 查询指定账本的账单列表；
- 支持按月份筛选；
- 查询单笔账单及分摊明细；
- 修改账单后重新计算分摊；
- 删除账单；
- 返回指定月份每位成员的累计分摊金额。

#### F5 结算报表（必做）

- 支持按月份范围统计；
- 返回每位成员的净余额；
- 返回余额总和校验结果；
- 返回可执行的转账方案；
- 通过官方测试 T11–T12。

#### F6 数据导出（必做）

导出指定账本和月份的 UTF-8 CSV 文件，文件名格式为：

```
export_YYYY-MM.csv
```

CSV 表头固定：

```
bill_id,title,bill_date,payer_name,amount_cents,method,participant_name,share_cents
```

同一 `bill_id` 的 `share_cents` 之和必须等于 `amount_cents`。

#### F7 多账本（必做）

- 可以创建账本；
- 可以查询、修改和删除账本；
- 账本列表显示名称和最近活跃时间；
- 每个账本下可以独立管理成员和账单；
- 切换账本后，只能访问当前账本的数据；
- 不同账本的数据文件必须相互隔离；
- 非空账本不得直接删除，应返回明确提示。

推荐存储方式：

```
data/
├── books.json
└── books/
    ├── book_001/
    │   ├── members.json
    │   └── bills.jsonl
    └── book_002/
        ├── members.json
        └── bills.jsonl
```

#### F8 文件并发保护（必做）

- 多个请求同时写入同一数据文件时不得损坏数据；
- 采用文件锁、锁文件或临时文件原子替换等方式；
- 写入失败时不得留下不完整文件；
- 存储层提供统一读写接口；
- 算法层不得感知文件锁；
- 通过并发写入测试。

#### F9 最少转账结算（必做）

- 实现 `minTransfers` 或 `min_transfers`；
- 返回转账次数最少的结算方案；
- 宿舍成员不超过 6 人；
- 应使用能够保证最优结果的算法，不能只依赖普通贪心策略；
- 通过测试用例 S01–S08。

#### F10 历史趋势（选做）

- 返回最近 6 个月的公共支出总额；
- 返回最近 6 个月的人均支出；
- 无数据月份按 0 返回；
- 后端返回结构化 JSON，不要求个人项目制作图表页面。

#### F11 凭证附件（选做）

- 每笔账单最多上传 3 个附件；
- 支持常见图片格式和 PDF；
- 文件保存在对应账本目录；
- 账单记录只保存附件元数据和相对路径；
- 提供上传、查询、下载和删除接口。

#### F12 结算确认与快照（选做）

- 可以根据账单范围创建结算快照；
- 快照保存成员净余额和转账方案；
- 记录成员确认状态；
- 全员确认后标记为已结清；
- 已锁定或已结清范围内的账单不得随意修改；
- 已结清账单不计入下一次未结算统计。

## 五、结对项目：前端功能

结对项目应选择一位成员已经通过个人项目验收的后端作为接口基础。

### 5.1 必做页面

#### P1 账本管理

- 展示账本列表；
- 创建和修改账本；
- 切换当前账本；
- 删除账本时显示二次确认和后端返回的保护提示；
- 切换账本后刷新成员、账单和结算数据。

#### P2 成员管理

- 展示当前账本成员；
- 新增和编辑成员；
- 显示在住或已退宿状态；
- 删除失败时展示明确原因；
- 对日期和姓名进行前端校验。

#### P3 账单录入

- 选择当前账本；
- 填写名称、金额、日期、参与人和垫付人；
- 根据分摊方式显示不同输入项；
- 保存前调用后端接口显示分摊预览；
- 展示字段级错误提示；
- 保存成功后跳转到账单详情或账单列表。

#### P4 账单列表与详情

- 按月份查询账单；
- 展示名称、金额、垫付人和分摊方式；
- 查看单笔账单及每人分摊明细；
- 编辑账单；
- 删除账单前二次确认；
- 展示当月成员累计分摊金额。

#### P5 结算页面

- 选择结算月份范围；
- 展示每位成员的应收或应付金额；
- 展示余额总和校验状态；
- 展示最少转账方案；
- 金额统一按“元”显示两位小数，但与后端交互时使用整数“分”。

#### P6 CSV 导出

- 选择账本和月份；
- 调用导出接口；
- 下载后端生成的 CSV 文件；
- 导出失败时展示错误信息。

#### P7 通用交互

- 提供清晰导航；
- 展示加载、空数据、成功和失败状态；
- 防止表单重复提交；
- 页面刷新后能够恢复或重新获取当前账本；
- 前端不得绕过后端校验；
- API 请求代码应统一封装。

### 5.2 选做页面

个人项目完成了相应选做后端功能时，可继续完成：

- F10 历史趋势图页面；
- F11 凭证上传、预览、下载和删除页面；
- F12 结算发起、成员确认和历史快照页面。

结对项目选做页面不作为基础功能完成度的前提，可计入加分项。

## 六、数据与接口约定

### 6.1 数据示例

`data/books.json`：

```json
{
  "books": [
    {
      "id": "book_001",
      "name": "3栋402宿舍",
      "updatedAt": "2026-03-28T10:30:00+08:00"
    }
  ]
}
```

`data/books/book_001/members.json`：

```json
{
  "members": [
    {
      "id": "m1",
      "name": "张三",
      "joinDate": "2026-01-01",
      "leaveDate": null
    }
  ]
}
```

`data/books/book_001/bills.jsonl`：

```json
{"id":"b001","bookId":"book_001","title":"3月电费","amountCents":18760,"date":"2026-03-28","method":"BY_DAYS","participants":["m1","m2","m3"],"payerId":"m1","period":{"start":"2026-03-01","end":"2026-03-31"}}
```

### 6.2 核心算法接口

算法函数必须：

- 不执行文件读写；
- 不依赖 HTTP 请求或响应对象；
- 不修改传入参数；
- 返回结构稳定、可直接测试的数据。

### 6.3 RESTful API

所有 JSON API 使用 `/api` 前缀。

| Method | Path                                                  | 说明          |
| ------ | ----------------------------------------------------- | ----------- |
| GET    | `/api/books`                                          | 查询账本列表      |
| POST   | `/api/books`                                          | 创建账本        |
| GET    | `/api/books/{bookId}`                                 | 查询账本详情      |
| PATCH  | `/api/books/{bookId}`                                 | 修改账本        |
| DELETE | `/api/books/{bookId}`                                 | 删除空账本       |
| GET    | `/api/books/{bookId}/members`                         | 查询成员        |
| POST   | `/api/books/{bookId}/members`                         | 新增成员        |
| GET    | `/api/books/{bookId}/members/{memberId}`              | 查询成员详情      |
| PATCH  | `/api/books/{bookId}/members/{memberId}`              | 修改成员        |
| DELETE | `/api/books/{bookId}/members/{memberId}`              | 删除未参与账单的成员  |
| GET    | `/api/books/{bookId}/bills?month=2026-03`             | 查询账单        |
| POST   | `/api/books/{bookId}/bills`                           | 创建账单        |
| GET    | `/api/books/{bookId}/bills/{billId}`                  | 查询账单详情      |
| PATCH  | `/api/books/{bookId}/bills/{billId}`                  | 修改账单        |
| DELETE | `/api/books/{bookId}/bills/{billId}`                  | 删除账单        |
| POST   | `/api/books/{bookId}/bill-previews`                   | 计算分摊预览，不保存  |
| POST   | `/api/books/{bookId}/settlement-plans`                | 生成指定范围的结算方案 |
| GET    | `/api/books/{bookId}/exports/bills.csv?month=2026-03` | 导出 CSV      |

选做接口可沿用同一资源风格，例如：

```
POST   /api/books/{bookId}/bills/{billId}/attachments
GET    /api/books/{bookId}/bills/{billId}/attachments/{attachmentId}
DELETE /api/books/{bookId}/bills/{billId}/attachments/{attachmentId}

GET    /api/books/{bookId}/statistics/monthly
POST   /api/books/{bookId}/settlements
PATCH  /api/books/{bookId}/settlements/{settlementId}/confirmations/{memberId}
```

### 6.4 HTTP 响应约定

建议使用以下状态码：

| 状态码   | 使用场景           |
| ----- | -------------- |
| `200` | 查询或修改成功        |
| `201` | 创建成功           |
| `204` | 删除成功且无响应体      |
| `400` | 请求格式或字段错误      |
| `404` | 资源不存在          |
| `409` | 删除保护、数据冲突或状态冲突 |
| `422` | 业务校验失败         |
| `500` | 未处理的服务器错误      |

错误响应格式应保持一致：

```
{
  "error": {
    "code": "MEMBER_IN_USE",
    "message": "该成员已参与账单，不能删除",
    "field": "memberId"
  }
}
```

## 七、测试用例

### 7.1 分摊测试 T01–T10

| 用例  | 场景        | 输入要点               | 期望结果                       |
| --- | --------- | ------------------ | -------------------------- |
| T01 | 均摊整除      | 60000 分，3 人        | `[20000, 20000, 20000]`    |
| T02 | 均摊不整除     | 10000 分，3 人        | `[3334, 3333, 3333]`       |
| T03 | 4 人均摊整除   | 10000 分，4 人        | `[2500, 2500, 2500, 2500]` |
| T04 | 4 人均摊不整除  | 10001 分，4 人        | `[2501, 2500, 2500, 2500]` |
| T05 | 按天全程在住    | 6200 分，天数 31:31    | `[3100, 3100]`             |
| T06 | 按天中途入住    | 6200 分，天数 31:21:31 | `[2316, 1569, 2315]`       |
| T07 | 按天中途退宿    | 7000 分，天数 10:30:30 | `[1000, 3000, 3000]`       |
| T08 | 一人有效天数为 0 | 5000 分，天数 0:10:10  | `[0, 2500, 2500]`          |
| T09 | 指定份额不整除   | 5000 分，权重 2:1      | `[3333, 1667]`             |
| T10 | 指定份额整除    | 4000 分，权重 3:1      | `[3000, 1000]`             |

### 7.2 余额测试 T11–T12

4 人账单：

| 账单 | 金额      | 方式     | 垫付人 | 参与人      |
| -- | ------- | ------ | --- | -------- |
| 电费 | 40000 分 | `EVEN` | 张三  | 4 人      |
| 水费 | 20000 分 | `EVEN` | 李四  | 4 人      |
| 纸巾 | 10000 分 | `EVEN` | 王五  | 张三、李四、王五 |

期望净余额：

```
张三 +21666
李四 +1667
王五 -8333
赵六 -15000
合计 0
```

- T11：净余额必须与期望结果一致；
- T12：执行转账方案后所有余额归零。

### 7.3 最少转账测试 S01–S08

| 用例  | 输入净余额（分）                       | 最少笔数 |
| --- | ------------------------------ | ---- |
| S01 | `[21666, 1667, -8333, -15000]` | 3    |
| S02 | `[-2000, 2000]`                | 1    |
| S03 | `[0, 0, 0]`                    | 0    |
| S04 | `[3000, -1000, -1000, -1000]`  | 3    |
| S05 | `[10000, 0, -10000]`           | 1    |
| S06 | `[1000, 1000, -1000, -1000]`   | 2    |
| S07 | `[5000, -5000, 0]`             | 1    |
| S08 | `[1, -1]`                      | 1    |

### 7.4 其他必测场景

- 创建两个账本后，成员和账单数据完全隔离；
- 非空账本删除返回 `409`；
- 已参与账单的成员删除返回 `409`；
- 修改成员日期后，按天分摊结果正确更新；
- 修改账单后，分摊和余额重新计算；
- 5 个并发写入请求执行后数据完整；
- 写入中断后原文件仍可读取；
- CSV 表头、金额和分摊守恒正确；
- 非法账本、成员和账单 ID 返回 `404`；
- 非法金额、日期和参与人返回 `400` 或 `422`。

## 八、里程碑建议

### 8.1 个人项目

| 天  | 任务                 |
| -- | ------------------ |
| D1 | 数据模型、目录结构、接口设计     |
| D2 | F7 多账本和 F1 成员管理    |
| D3 | F2 账单录入和 F4 账单维护   |
| D4 | F3 分摊算法和测试         |
| D5 | F5、F9 结算算法         |
| D6 | F6 导出、F8 并发保护、选做功能 |
| D7 | 接口测试、README、自查和打包  |

### 8.2 结对项目

| 天  | 任务                 |
| -- | ------------------ |
| D1 | 选择后端版本、确认接口、拆分页面任务 |
| D2 | 账本和成员页面            |
| D3 | 账单录入与分摊预览          |
| D4 | 账单列表与详情                   |
| D5 | 结算和导出页面            |
| D6 | 联调、交叉评审和回归测试       |
| D7 | 修复问题、完善文档和打包       |

## 九、交付物

### 9.1 个人项目

提交压缩包：

```
学号-姓名-dormbill-backend.zip
```

内容包括：

- 完整 Git 仓库和提交历史；
- 后端源码；
- 文本数据目录示例；
- `README.md`；
- `docs/API.md`；
- `docs/SELF_CHECK.md`。

README 至少说明：

- 技术栈；
- 安装和启动命令；
- 测试命令；
- 数据目录；
- API 文档位置；
- 并发写入策略；
- 已完成的必做和选做功能。

### 9.2 结对项目

提交压缩包：

```
学号-姓名-学号-姓名-dormbill-web.zip
```

内容包括：

- 包含后端和前端的完整源代码；
- 完整 Git 仓库和双人提交历史；
- `docs/PAIRLOG.md（不限文字格式，记录分工和个人实质性提交）`；
- `docs/REVIEW.md（不限文字格式，记录交叉评审结果和修改情况）`；
- `docs/SELF_CHECK.md`；
- 更新后的 README。

## 十、评分标准

评分以可运行结果为主。

### 10.1 个人项目评分表（100 分）

#### A. 必做功能正确性（60 分）

| 项目                   | 分值 | 判定方式               |
| -------------------- | -- | ------------------ |
| A1 分摊算法 T01–T10      | 20 | 每通过 1 个用例得 2 分     |
| A2 余额与可行结算 T11–T12   | 6  | T11、T12 各 3 分      |
| A3 最少转账 S01–S08      | 16 | 每通过 1 个用例得 2 分     |
| A4 多账本创建、维护与数据隔离     | 8  | 创建、切换、隔离、删除保护各 2 分 |
| A5 成员和账单 RESTful API | 6  | 按接口测试结果评分          |
| A6 CSV 导出            | 4  | 表头、编码、筛选和金额守恒正确    |

#### B. 程序源代码理解能力（40 分）

| 项目                | 分值 | 判定方式   |
| ----------------- | -- | ------ |
| 均摊的核心算法代码理解能力     | 10 | 助教现场问答 |
| 查询单笔账单及分摊明细代码理解能力 | 10 | 助教现场问答 |
| 最少转账结算代码理解能力      | 10 | 助教现场问答 |
| CSV 导出代码理解能力      | 10 | 助教现场问答 |

#### 选做加分

| 选做功能        | 加分       |
| ----------- | -------- |
| F10 历史趋势    | 最高 1.5 分 |
| F11 凭证附件    | 最高 2 分   |
| F12 结算确认与快照 | 最高 1.5 分 |

选做加分后，个人项目总分最高不超过 100 分。

### 10.2 结对项目评分表（100 分）

#### A. 前端功能完成度（50 分）

所有界面除功能外，界面必须符合主流手机的竖屏显示要求。不符合的，每个界面扣1分。

| 项目           | 分值 | 判定方式                     |
| ------------ | -- | ------------------------ |
| A1 账本管理      | 8  | 通过界面创建、修改、切换和删除保护交互完整    |
| A2 成员管理      | 7  | 通过界面查看列表、新增、编辑、删除和状态显示完整 |
| A3 账单录入与分摊预览 | 10 | 通过界面创建动态表单、校验、预览和保存完整    |
| A4 账单列表与详情   | 10 | 通过界面月份筛选、详情、编辑、删除和累计分摊完整 |
| A5 结算页面      | 8  | 通过界面范围选择、净余额和最少转账方案展示正确  |
| A6 CSV 导出    | 4  | 通过界面能选择月份并下载正确文件         |
| A7 通用交互状态    | 3  | 加载、空数据、错误、成功和防重复提交完整     |

#### B. 程序源代码理解能力（40 分）

| 项目                                  | 分值 | 判定方式                  |
| ----------------------------------- | -- | --------------------- |
| 按天摊代码理解能力                           | 10 | 助教现场问答，结对编程中任何一人回答即可。 |
| 指定份额代码理解能力                          | 10 | 助教现场问答，结对编程中任何一人回答即可。 |
| 三种分摊方式录入账单界面与后端交互时提交的请求和返回响应的代码理解能力 | 10 | 助教现场问答，结对编程中任何一人回答即可。 |
| 导出CSV界面与后端交互时提交的请求和返回响应代码理解能力       | 10 | 助教现场问答，结对编程中任何一人回答即可。 |

#### C. 结对协作与交付（10 分）

| 项目        | 分值 | 判定方式                         |
| --------- | -- | ---------------------------- |
| C1 结对记录   | 5  | 记录分工、个人实质性代码提交（`PAIRLOG.md`） |
| C3 交叉评审记录 | 5  | 记录交叉评审的结果和修改情况（`REVIEW.md`）  |
|           |    |                              |

#### 选做加分

| 选做页面          | 加分       |
| ------------- | -------- |
| F10 历史趋势图     | 最高 1.5 分 |
| F11 凭证管理页面    | 最高 2 分   |
| F12 结算确认与快照页面 | 最高 1.5 分 |

选做加分后，结对项目总分最高仍为 100 分。

### 10.3 评分原则

1. 必做功能缺失时，直接扣除对应功能分。
2. 接口或页面无法运行时，不得仅根据代码文件存在情况给满分。
3. 指定的代码逻辑解释，需逐行解释，任何一条语句无法解释清楚与业务功能关系时，扣除对应的分数。
4. 选做功能只有在可运行、可测试时才能加分。
5. 严重数据损坏、金额不守恒或跨账本数据混用属于关键缺陷，对应项目不得分。
6. 最终成绩不超过 100 分。

### 附录：SELF_CHECK.md 模板

```markdown
# 提交自查表

## 个人项目

- [ ] F1–F9已完成
- [ ] 完成了 F10、F11、F12 中的某项或几项
- [ ] 可以创建多个账本
- [ ] 每个账本可以独立管理成员和账单
- [ ] 不同账本数据相互隔离
- [ ] T01–T12 全部通过
- [ ] S01–S08 全部通过
- [ ] 并发写入测试通过
- [ ] 金额字段使用整数分和 Cents 后缀
- [ ] RESTful API 可正常访问
- [ ] CSV 导出结果正确
- [ ] README 和 API 文档完整
- [ ] 解压后可以按 README 完成安装、测试和启动

## 结对项目

- [ ] 账本、成员、账单、结算和导出页面完整
- [ ] 分摊预览可以正常使用
- [ ] 加载、空数据、成功和失败状态完整
- [ ] 页面刷新和账本切换后数据正确
- [ ] 前端未直接读写数据文件
- [ ] 个人项目后端测试仍全部通过
- [ ] 两位成员均有有效代码提交
- [ ] PAIRLOG 和 REVIEW 已完成
- [ ] 前后端可以按 README 正常启动
```
